Main
----
SONNET (String)          - the full sonnet text

main()                    - runs the game loop, reads guesses, tracks score
isCorrectGuess(guess, word)      - checks if guess matches word
buildTruncatedSonnet(words, i)   - builds sonnet text cut off at word i